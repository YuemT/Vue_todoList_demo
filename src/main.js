// The Vue build version to load with the `import` command
// (runtime-only or standalone) has been set in webpa
import Vue from 'vue'
import app from './components/app.vue'

new Vue({
  el:'#test',
  render:h => h(app)
})

